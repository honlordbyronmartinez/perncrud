import React from 'react';

const projects = () => (
    <div>Projects Module</div>
);

export default {
    routeProps: {
        path: '/projects',
        component: Projects
    },
    name: 'Projects',
}